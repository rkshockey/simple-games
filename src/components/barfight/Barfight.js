import React, { useState } from 'react';
import styled from 'styled-components';

function Barfight (props) {
    const [pHP, setPHP] = useState(200)
    const [eHP, setEHP] = useState(200)

    return null
}

export default Barfight